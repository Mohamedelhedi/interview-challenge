name: My First Pipeline

on:
  push:
    branches: [ main ]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Print a Greeting
        run: echo "Hello World! My pipeline is starting!"
